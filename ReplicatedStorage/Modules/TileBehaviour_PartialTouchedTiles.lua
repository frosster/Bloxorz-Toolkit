-- NOTE: This should be parented under the TileBehaviour module with the name PartialTouchedTiles

-- PartialTouchedTiles: stores the dictionary of tile names that can be activated if the block is partially touching the tile

return {
	Checkpoint = true,
	SwitchLight = true,
}
